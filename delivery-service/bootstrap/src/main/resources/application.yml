spring:
  datasource:
    url: jdbc:mariadb://jinhome-server.tplinkdns.com:3306/delivery_service?useSSL=false&serverTimezone=UTC&characterEncoding=UTF-8
    username: delivery
    password: asd123
    driver-class-name: org.mariadb.jdbc.Driver

  output:
    ansi:
      enabled: always   # 실행 시 콘솔 ANSI 색상 출력 활성화

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MariaDBDialect
  application:
    name: delivery-service

server:
  port: 8082
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/
